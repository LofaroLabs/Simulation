To load specific environment file: Modify virtualhubo.py file (in etc/hubo-ach) with sudo priveleges and text editor. This can be done by opening gedit at terminal with "gksudo geddit" 

To make sure the objects are loaded correctly the 'files' and 'objects' directories must must be under this master "peg_in_hole_and_valve_turn_complete" directory.
